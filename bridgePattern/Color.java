package bridgePattern;
//Bridge design pattern
//decouple an abstraction from its implementation so that 
//the two can vary independently. This means to create a 
//bridge interface that uses OOP principles to separate out 
//responsibilities into different abstract classes.
public interface Color {
	
	public void applyColor();
}
